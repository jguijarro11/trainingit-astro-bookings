{
  "name": "trainingit-astro-bookings",
  "version": "1.2.0",
  "description": "Backend API for rocket launch bookings",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/index.js",
    "test": "playwright test",
    "test:dev": "vitest watch",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.2",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
