# Code

## Role

Act as a senior software developer.

## Task

Implment the functionality described in the spec file provided.

Do not write teasts or documentation, just the code.

## Context

A file named `specs/rockets.spec.md` with the specification to implement the rockets API.

Ask for the spec file if you don't have access to it.

### Code Guidelines

- Use ES modules (`import`/`export`) instead of CommonJS.
- Use strict typing and avoid using `any`.
- Declare `types` for data structures in its own file.
- Define `interfaces` for class contract behavior in its own file.
- Avoid `null` and `undefined` where possible; prefer optional properties.
- Leverage TypeScript's utility types (e.g., `Partial`, `Pick`, `Omit`).

## Steps to follow:
1. **Understand the Spec**:
  - Read the context to grasp the requirements.
2. **Break It Down**:
  - Divide the funcionallity into smaller components.
3. **Have a Plan**:
  - Genarate the steps to implement
4. **Write the Code**:
  - Write the minimum code necessary to fulfill the plan.

## Output checklist:
- [ ] Modified or newly created code files as specificed in the implementation plan.
